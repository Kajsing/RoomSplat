# Milestone 2 Implementation Log

## Completed

- Implemented local project storage under the configured data directory.
- Added `POST /projects` and `GET /projects` API routes.
- Added project schemas and backend tests for folder creation, metadata, validation, and API create/list.
- Wired the React frontend to list and create projects.
- Updated README, project format docs, `.env.example`, and `DOCUMENTATION.md`.

## Validation

- Passed: `$env:PYTHONPATH='backend'; C:\Users\ckajs\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m pytest backend/tests pipeline/tests`
- Passed: `C:\Users\ckajs\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe node_modules\vite\bin\vite.js build` from `frontend/` with bundled Node on PATH.

## Assumptions

- Project folder names use UUID project IDs rather than user-provided names to avoid path traversal and filesystem naming problems.
- Project metadata lives in `metadata/project.json`.
- `ROOMSPLAT_DATA_DIR` is the explicit app-specific data directory variable, with `DATA_DIR` kept as fallback compatibility.

## Deviations

- `npm --prefix frontend run build` could not be used because `npm` is unavailable on PATH in this shell. Bundled `pnpm` can install dependencies, but its build-script approval checks make `pnpm run build` unreliable here after removing generated pnpm project files. Used the installed Vite CLI directly for final frontend validation.

## Milestone 3 inspection

- `backend/app/services/video_import.py`, `backend/app/services/frame_extraction.py`, `pipeline/scripts/inspect_video.py`, `pipeline/scripts/extract_frames.py`, and `pipeline/tests/test_frame_extraction.py` are placeholders.
- Next slice should implement local video import into `input/`, import metadata, deterministic frame extraction, and synthetic/tiny media tests before any reconstruction work.
