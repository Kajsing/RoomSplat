# Milestone 3 Implementation Log

## Completed

- Implemented raw video upload into project `input/` folders.
- Added video import metadata under `metadata/video_import.json`.
- Implemented deterministic frame extraction for animated GIF fixtures using Pillow.
- Added optional `ffmpeg` extraction path for general video extensions when `ffmpeg` is available locally.
- Added frame extraction metadata under `metadata/frame_extraction.json`.
- Added backend routes for upload and synchronous extraction.
- Added pipeline CLI scripts for inspecting and extracting deterministic fixtures.
- Replaced placeholder tests with synthetic tiny media tests.
- Wired frontend project selection, upload, extraction controls, and extraction status.
- Updated docs and config examples.

## Validation

- Passed: `$env:PYTHONPATH='backend'; C:\Users\ckajs\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe -m pytest backend/tests pipeline/tests`
- Passed: `C:\Users\ckajs\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe node_modules\vite\bin\vite.js build` from `frontend/` with bundled Node on PATH.

## Assumptions

- Synchronous extraction is acceptable for Milestone 3; Milestone 5 will move long-running extraction/reconstruction into the job system.
- Animated GIF fixtures are acceptable as deterministic tiny media tests. Real-world video formats are supported through an optional local `ffmpeg` path.
- Uploaded filenames are sanitized and stored with a short random suffix to avoid collisions.

## Deviations

- `npm --prefix frontend run build` still cannot be used in this shell because `npm` is not on PATH. Direct Vite CLI validation was used as in Milestone 2.
- No reconstruction work was started.

## Next slice

- Milestone 4 should evaluate the local Windows-native reconstruction path and document exact dependencies, commands, and output contracts before integration.
