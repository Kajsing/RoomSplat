# Copy this file to .env for local development. Process environment variables
# override values in .env. Generated videos, frames, artifacts, and exports live here.
ROOMSPLAT_DATA_DIR=./data

# Backward-compatible alias used only when ROOMSPLAT_DATA_DIR is unset.
DATA_DIR=./data

# Optional ffmpeg executable path. Leave empty to use ffmpeg from PATH.
ROOMSPLAT_FFMPEG_PATH=

# Backward-compatible alias used only when ROOMSPLAT_FFMPEG_PATH is unset.
FFMPEG_PATH=

# Optional COLMAP executable path for real sparse point-cloud reconstruction.
# Leave empty to use colmap.exe from PATH.
# Nerfstudio 1.1.5 process-data currently expects COLMAP's older SiftExtraction.use_gpu flag;
# on Windows, COLMAP 3.9.1 no-CUDA is compatible, while COLMAP 4.1 may fail for splat process-data.
ROOMSPLAT_COLMAP_PATH=

# Backward-compatible alias used only when ROOMSPLAT_COLMAP_PATH is unset.
COLMAP_PATH=

# Optional Nerfstudio CLI configuration for real Gaussian splat reconstruction.
# Leave empty to use ns-process-data, ns-train, and ns-export from PATH.
# Set ROOMSPLAT_NERFSTUDIO_BIN_DIR to the Scripts/bin folder of an isolated Nerfstudio environment,
# or set the individual executable paths below.
# Optionally set ROOMSPLAT_NERFSTUDIO_PYTHON_PATH to that environment's python.exe so readiness
# can report torch/nerfstudio/gsplat import failures, including Windows Application Control blocks.
# Windows note: install Nerfstudio in an isolated micromamba/conda env. RoomSplat will prepend
# that env's Scripts/bin/Library/bin paths, set UTF-8 subprocess output, infer CUDA_HOME for
# both env/bin/nvcc.exe and env/Library/bin/nvcc.exe layouts, and ask Nerfstudio's viewer to
# quit when training completes. The first successful local stack used Python 3.10,
# PyTorch 2.1.2+cu118, Nerfstudio 1.1.5, and precompiled gsplat==1.4.0+pt21cu118.
ROOMSPLAT_NERFSTUDIO_BIN_DIR=
ROOMSPLAT_NS_PROCESS_DATA_PATH=
ROOMSPLAT_NS_TRAIN_PATH=
ROOMSPLAT_NS_EXPORT_PATH=
ROOMSPLAT_NERFSTUDIO_PYTHON_PATH=

# Optional learned-geometry runtime smoke path.
# RoomSplat never downloads checkpoints automatically. Put model files under
# ROOMSPLAT_LEARNED_MODEL_ROOT, set the relative checkpoint name/path, and set
# the expected SHA-256 before running a learned runtime job.
# ROOMSPLAT_LEARNED_RUNTIME_COMMAND points to a local adapter executable or
# wrapper script. ROOMSPLAT_LEARNED_RUNTIME_ARGS can hold fixed wrapper args,
# such as a Python script path when COMMAND is python.exe.
# The adapter is invoked with:
# --frames-dir --output-dir --checkpoint --max-frames --image-max-size --precision --frame-index-map
ROOMSPLAT_LEARNED_RUNTIME_COMMAND=
ROOMSPLAT_LEARNED_RUNTIME_ARGS=
ROOMSPLAT_LEARNED_RUNTIME_PYTHON_PATH=
ROOMSPLAT_LEARNED_MODEL_ROOT=./data/models
ROOMSPLAT_LEARNED_CHECKPOINT_PATH=
ROOMSPLAT_LEARNED_CHECKPOINT_SHA256=
ROOMSPLAT_LEARNED_CACHE_DIR=./data/cache/learned-runtime
ROOMSPLAT_LEARNED_MIN_FREE_VRAM_MB=10000
ROOMSPLAT_LEARNED_RUNTIME_TIMEOUT_SECONDS=1800

# VGGT-first example after manually installing VGGT in a local environment:
# ROOMSPLAT_LEARNED_RUNTIME_COMMAND=C:\path\to\vggt-env\python.exe
# ROOMSPLAT_LEARNED_RUNTIME_ARGS=pipeline\scripts\run_vggt_runtime.py
# ROOMSPLAT_LEARNED_RUNTIME_PYTHON_PATH=C:\path\to\vggt-env\python.exe
# ROOMSPLAT_LEARNED_MODEL_ROOT=C:\project\RoomSplat\data\models
# ROOMSPLAT_LEARNED_CHECKPOINT_PATH=vggt\model.pt
# ROOMSPLAT_LEARNED_CHECKPOINT_SHA256=<Get-FileHash output>

# Maximum accepted upload size in megabytes. Default in code: 2048 MB.
ROOMSPLAT_MAX_UPLOAD_MB=2048

# Startup convention for local development. Bind to 127.0.0.1 unless you add auth.
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000

# Frontend build-time backend URL. Vite only exposes variables with VITE_ prefix.
VITE_ROOMSPLAT_API_URL=http://127.0.0.1:8000

# Optional ffmpeg timeout for frame extraction jobs, in seconds.
ROOMSPLAT_FFMPEG_TIMEOUT_SECONDS=1800
